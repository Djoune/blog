{% extends 'base.html.twig' %}

{% block title %}Edit Tag{% endblock %}

{% block body %}
    <div class="container">
        <div class="row my-4">
            <h1 class="col">Edit Tag</h1>
            <a class="text-right" href="{{ path('tag_index') }}">back to list</a>
        </div>

        {{ include('tag/_form.html.twig', {'button_label': 'Update'}) }}

        <div class="mt-5">
            {{ include('tag/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
