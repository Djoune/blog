{{ form_start(form) }}
<div class="row">
    <div class="col">{{ form_row(form.name) }}</div>
</div>
<div class="row">
    <div class="col">{{ form_row(form.articles) }}</div>
</div>

<button type="submit" class="btn btn-primary">{{ button|default('Save') }}</button>
{{ form_end(form) }}
